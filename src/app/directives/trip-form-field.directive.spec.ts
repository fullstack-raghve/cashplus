import { TripFormFieldDirective } from './trip-form-field.directive';

describe('TripFormFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new TripFormFieldDirective();
    expect(directive).toBeTruthy();
  });
});
