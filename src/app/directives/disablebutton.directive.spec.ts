import { DisablebuttonDirective } from './disablebutton.directive';

describe('DisablebuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new DisablebuttonDirective();
    expect(directive).toBeTruthy();
  });
});
