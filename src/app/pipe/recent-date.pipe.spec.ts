import { RecentDatePipe } from './recent-date.pipe';

describe('RecentDatePipe', () => {
  it('create an instance', () => {
    const pipe = new RecentDatePipe();
    expect(pipe).toBeTruthy();
  });
});
