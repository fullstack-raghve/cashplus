import { MulticitydatesPipe } from './multicitydates.pipe';

describe('MulticitydatesPipe', () => {
  it('create an instance', () => {
    const pipe = new MulticitydatesPipe();
    expect(pipe).toBeTruthy();
  });
});
