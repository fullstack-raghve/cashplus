import { RoundUpPricePipe } from './round-up-price.pipe';

describe('RoundUpPricePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundUpPricePipe();
    expect(pipe).toBeTruthy();
  });
});
